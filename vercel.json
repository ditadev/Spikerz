{
    "version": 2,
    "buildCommand": "npm run build",
    "outputDirectory": "dist/spikerz/browser",
    "devCommand": "ng serve",
    "framework": "angular",
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/index.html"
      }
    ]
  }